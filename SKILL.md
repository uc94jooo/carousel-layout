---
name: carousel-layout
description: >
  IG 輪播圖設計系統「排版小幫手」。把任何內容（文章、文案包、
  逐字稿、筆記）編頁並套用 uncle.johs 品牌版式，產出 1080×1350 的輪播頁面 HTML。
  內建三主題：螢光筆灰色（思考類）／螢光暖色（生活感類）／螢光粉色（感情輕生活類）。
  Use when the user explicitly calls or summons '排版小幫手'，
  或要求「做成輪播圖」「套版」「排輪播」「用我的版型做圖」「產輪播頁面」時。
---

# 排版小幫手：輪播設計系統

把內容編頁、套用品牌版式、產出輪播頁面。核心原則一句話：**版型自由，品牌鎖死**。

## 最高原則：溝通力優先（2026-07-27 定）

字數預算、行數上限、版型文法全是手段，目的是每一頁的閱讀感與溝通力。思考順序固定：
1. 先問「這篇文章的閱讀感需要保留什麼」——敘事節拍、關鍵對白、反問句、收尾力道
2. 再問「用什麼版型組合、多少頁數，能承載這些東西」（技能不設總頁數上限）
3. 字數預算只在**句子層**作用（同一句怎麼斷行、怎麼收短）；不准升級為**內容層**的刪減依據

遇到裝不下，答案永遠是「多開頁、換版型、重新編排」，不是「刪掉這段」。
無可避免的濃縮或刪減，一律列進「濃縮與刪減申報」（附在人讀版工單檔尾，
見第 2 步工單三檔制），由使用者裁決；不准默默做掉。

## 唯一事實來源（每次執行前必讀）

規格庫位於技能根目錄的 `system/`：

| 檔案 | 內容 | 何時讀 |
|------|------|--------|
| `design-tokens.md` | 色票、金萱字型層級、間距、斷行字數預算、留白規則 | 每次必讀 |
| `components.md` | C0～C26 元件文法（含尺度檔位標籤、變化開放原則、標本制） | 每次必讀 |
| `page-layouts.md` | 任務實例庫（L1～L13）＋組裝原則＋敘事順序 | 編頁時讀 |
| `templates/` | 版型 HTML 模板＋`assets/`（tokens.css、base.css 元件庫） | 填稿時用 |

規格與本技能衝突時，以規格庫為準；發現規格過時，先回報使用者再動規格。

## 兩層約束：什麼能動、什麼不能動

**品牌層（硬約束，任何版型都必須遵守）**：
- 色票只有五色（灰白米黑＋螢光黃）；彩色只准出現在插畫內部
- 螢光筆規則：詞級每頁最多 3 處、句級每頁最多 1 組且並存時詞級減量；全系列只有黃色
- 金萱字型層級：每頁最多三個字級；多行大標一律 64px，單行才可 76px+
- 字數預算：行尾不留 1～2 個孤字；超預算改寫句子，不縮字級
- 每頁固定頁尾 `IG @uncle.johs`；每頁只講一件事
- 插畫：扁平限色風格，佔比不超過 1/3（滿版頁例外）

**版型層（軟參考，可跳出）**：
- L1～L11 是參考庫不是圍牆：庫內命中就用（省時、穩定），不命中就用 `base.css`
  的元件自組新版型
- 自組版型只能用既有元件類別與 CSS 變數拼裝；效果好的自組版型，經使用者同意後
  存為新模板入庫

## 工作流程

### 第 1 步：收稿與讀規格
- 讀齊上表四個規格來源
- 確認素材：內容全文、敘事結構（概念教學或故事案例，判斷不了就問）、頁數期望
- 圖片改作（使用者丟成品圖來換主題／改字）：直接讀圖上文字 → 對版型 → 開工單
  → 填稿，一律按重建處理。不做全專案搜尋找原始檔（grep 內容、翻資料夾都不准）；
  只有使用者主動給原始檔路徑，才走改檔捷徑（2026-07-23 定）
- 選主題（整組一致，拿不準就各渲染一頁封面給使用者選）：主題清單以
  `themes/`（技能根目錄） 資料夾為準（一主題一檔，規格在檔頭註解），
  目前：灰（預設）／暖 `warm`／粉 `pink`。新增主題照 design-tokens.md 的 SOP，
  主題名屬使用者決策，不得自創
- 選輸出選配（規則見 design-tokens.md「輸出選配」）：
  線框——無框（預設）或有框（class `lined`，內緣細墨線框＋頁尾隔間）；
  格線——主題預設、全部有格（`grid-paper`）或全部無格（`no-grid`）。
  可做互動試衣間讓使用者即時切換挑選。試衣間用共用腳本組裝（與 export.py
  同模式，全技能只有一份，勿複製進專案資料夾）：
  ```
  python3 <技能根目錄>/system/templates/build-board.py \
    P1.html P2.html ... [--title 標題] [--default-theme warm] [--default-grid on]
  ```
  純標準函式庫（argparse/glob/base64…），不需 playwright，任何 python3 都能跑。
  board.html 產在頁面資料夾；頁內圖片自動 base64 內嵌，board 放哪都不斷圖。
  第 4 步會用此指令自動產一份預設版，見第 4 步說明

### 第 2 步：編頁開工單
- **素材盤點（每篇必做，2026-07-27 定）**：編頁前先對全文逐條回答判別問句，
  把藏在行文裡的「可視覺化隱性結構」全部挖出來：
  1. 有沒有多個選項被排成一條引導路徑？→ C9 三欄／C18 膠囊列
  2. 有沒有同一件事的兩份說法，一份被放大、一份被藏起？→ C6b 非等比對照／L13 大字小字頁
  3. 有沒有人在兩條路之間做選擇（「如果 A 就 X、但選了 B 所以 Y」）？→ C19 岔路圖
  4. 有沒有 3 步以上的因果推進？→ C10／C17／L9
  5. 有沒有同一個詞被偷換成兩種定義？→ C21 同詞雙義卡
  6. 有沒有「程度、界線、一線之隔」類的金句？→ C20 光譜條
  7. 有沒有前後認知變化（知道真相前 vs 後）？→ C6 對照卡
  8. 有沒有輪替、循環、回到原點的結構（A 煩了做 B、B 煩了做 C、C 又回 A）？
     → C23 循環圖
  9. 還有沒有可畫的結構，是庫內**沒有合身容器**的？→ 不准硬塞進既有元件；
     寫成「新形狀提案」（描述長相與結構）交使用者裁決是否入庫
  標籤鐵則：每組素材的分析標籤必須寫「機制」，不准只寫名詞
  （正例：「放大缺點：耗電快」；反例：「白色手機」）。
  盤點結果（素材｜結構類型｜分析標籤｜對應元件｜尺度｜建議用頁）固定寫入
  「重點-建議-待決」檔（見本步末的工單三檔制）交使用者裁決挑選；一組都沒找到
  就明寫「沒找到」——這個回報本身就是掃描確實發生過的證據。兩條防盲裁規則（2026-07-27 定）：
  （a）「對應元件」欄禁用裸代號，必須附一行白話描述（長什麼樣、佔多大版面），
  讓使用者不翻庫也能想像畫面；（b）「尺度」欄明寫「整頁主角」或「頁內配件」——
  同一素材做成頁內小條和做成整頁，溝通力差一個量級，這個決定必須攤給使用者裁；
  全文機制核心的素材，預設建議升級為整頁
- 按敘事順序拆頁，每頁一件事；一頁塞兩件事就拆成兩頁
- 頁標非必備（規則見 design-tokens.md 版面通則）：每頁先問「沒有頁標會不會更好」，
  不反射性下標題；敘事連續頁、滿版情境頁、金句頁傾向無頁標，工單頁標欄寫「無」。
  三個具體觸發（2026-07-27 挪自 V2）：同場景延續頁（跟上一頁是同一幕戲，標題會把戲
  切斷）；原文開頭自帶轉折或鉤子（**標題跟首句重複資訊時一律刪標題**）；收尾餘韻頁
  （讓內容自己收，不用標題預告）
- 每頁先填一張結構化工單（格式與範例見技能根目錄 `manifest-format.md`）——這份是
  內部事實依據，永遠先產生、永遠保留，供第 4 步填模板用
- 選版順序（2026-07-28 物件制）：先定這頁的**溝通任務** → 查 page-layouts.md 任務
  實例庫，有實例就照抄或改排 → 沒有就用物件自組（挑物件看 components.md 的尺度檔位標籤，
  標【備用】者素材命中才用；疊法照 page-layouts.md 組裝原則）。工單的版型欄可填實例代號（L7），
  也可直接寫堆疊序列＋檔位（例：頁標＋C23 循環圖｜半頁＋收束句）；物件的變化
  （數量、排列、連接、檔位）一律寫進工單交裁決
- 所有 slot 文字照該版型的字數預算斷好句；螢光筆位置在工單中標明
- 結構化工單填完後，照 `manifest-format.md`「人讀版」章節的規則轉譯成人讀版
  （markdown 標題／粗體／引用／行內代碼模擬大小字層次）——**這是預設要輸出給
  使用者看的版本，不是結構化工單原文**（2026-07-27 定）
- **工單三檔制（2026-07-28 定）**：每個任務固定產出三個 .md 檔——
  ①「工單-結構化.md」：內部事實依據，欄位格式照 `manifest-format.md`，永遠先產生、
  永遠保留，第 4 步填模板照它施工；
  ②「工單-人讀版.md」：四行摘要＋各頁工單，檔尾依序附「濃縮與刪減申報」
  「順稿紀錄」「待裁決事項」三段（2026-07-28 定），是預設交給使用者過目、
  可直接在檔案上修改的版本；
  ③「重點-建議-待決.md」：只收素材盤點與新形狀提案，不混進各頁工單。
  建立時機：第 2 步編頁完成、進第 3 步裁決**之前**，三檔一次建齊並存進任務資料夾
  （位置見第 4 步存檔路徑），讓使用者可直接改檔；裁決有改動時三檔同步更新
  （含頁碼重編），更新後在原路徑覆寫，不另開新檔名
- 三檔建齊後，照下方「狀態文件」規則寫入 `階段: 工單階段`

### 第 3 步：⏸ 人類介入暫停點：工單裁決
把「人讀版」交給使用者過目，並附上「重點-建議-待決.md」的路徑（結構化工單留著
備用，不主動展示，使用者要看原始欄位格式才給）。使用者可改：版型選擇、斷句、
螢光筆位置、頁數；也可直接在檔案上改，改完叫排版小幫手讀檔。裁決有改動時，
三檔同步更新。
交付人讀版時，一併附上模板總覽頁路徑（`<技能根目錄>/system/templates/index.html`，
file:// 直開，全部版型活縮圖），讓使用者裁決元件時看得到形狀；工單中提到總覽頁
沒有的元件（備用區未建模板者、新形狀提案），可現場渲染一張示意圖給使用者看
（export.py，每張數秒，成本可忽略）。
**未經裁決不得進入填稿。**

### 第 4 步：填模板
- 庫內版型：複製對應 `L*.html`／`C*.html` 改內容；自組版型：新建 HTML 引用 `assets/` 兩個 CSS
- 平行施工（2026-07-27 定）：頁數多（約 5 頁以上）時可開子代理平行填頁——每個
  子代理拿「單頁的結構化工單＋規格庫＋對應模板」獨立施工。頁與頁的連貫性已由
  工單「銜接」欄在編頁階段鎖定，平行不影響；但第 5 步自檢與第 6 步 PPTX 轉檔仍由
  主流程統一執行，不交給子代理分頭做
- 填完跑 `python3 templates/build-index.py` 重建總覽頁
- **board.html 隨頁面一起產出（2026-07-30 使用者定案，改列預設步驟）**：全部頁面填完後，
  跟著跑一次 `build-board.py`（見第 1 步指令；不需 playwright，純標準函式庫，直接跑），
  和各頁 HTML 一起交付，讓使用者不用另外開口就能即時切換主題／框線／格線比較。
  裁決有改動、頁面重填時，比照三檔同步更新的精神一併重跑覆寫，不另開新檔名
- 存檔路徑（2026-07-28 改）：所有產出（工單三檔 .md、頁面 HTML、
  assets/themes 複本、board.html、PPTX，選配的 PNG）一律存
  `~/Documents/Obsidian Vault/_排版小幫手/<YYYY-MM-DD 專案名>/`（日期前綴＝專案製作日）；
  使用者若沒有 Obsidian Vault，改存 AI 專案根目錄的
  `_排版小幫手/<YYYY-MM-DD 專案名>/`（例：`~/Desktop/Claude/_排版小幫手/...`）。
  **不再存** `<專案根目錄>/carousel-layout/`。一個任務一個子資料夾，資料夾名沿用
  任務或文章的既有名稱，不自創新名。OBS 受 TCC 保護，寫入前先取得目錄授權；
  Write 工具寫不進 OBS 時，先寫到暫存區再用 shell 複製過去
- fonts 不逐專案複製（2026-07-28 定）：金萱走 tokens.css 的 `local()` 吃系統已安裝
  字型，字型檔只在技能目錄 `system/templates/fonts/` 保留一份當備援
- 頁面 HTML＋board.html 產出後，照下方「狀態文件」規則寫入 `階段: BOARD.HTML階段`

### 狀態文件（供「第二大腦/寫作進度」調閱，2026-07-31 定案）

「第二大腦」的 writing-progress 技能會來查詢每個任務目前的階段。與其讓它用檔案
類型去猜（不可靠——已實測踩過用「有沒有資料夾」誤判完成／封存的坑），改由本技能
（產出方）主動寫一份隱藏狀態檔，第二大腦只需要讀它，不用再猜。

- 檔案位置：`<任務資料夾>/.progress.json`（隱藏檔，任務資料夾即第 4 步存檔路徑
  `_排版小幫手/<YYYY-MM-DD 專案名>/`）
- 格式：
  ```json
  {
    "階段": "工單階段",
    "更新時間": "2026-07-31T16:00",
    "備註": ""
  }
  ```
- 「階段」欄位只能是三個固定字串之一，按產線順序覆寫、不倒退：
  `工單階段` → `BOARD.HTML階段` → `PPT階段`
- 三個更新時機（各自覆寫整份檔案，不是新增）：
  1. 第 2 步工單三檔建齊時 → 寫 `工單階段`
  2. 第 4 步頁面 HTML＋board.html 產出後 → 寫 `BOARD.HTML階段`
  3. 第 6 步 PPTX 轉檔完成後 → 寫 `PPT階段`
- 「備註」欄位選填，用於本技能想特別提醒第二大腦的事（例如同一頁做了多個變體版本）；
  沒有要說的就留空字串，不要為了填東西硬掰。
- 這份檔案只有本技能會寫，第二大腦只讀不改；任務資料夾被刪除或搬移導致這份檔案
  消失，屬於第二大腦那邊「消失待確認」的情境，不是本技能要處理的事。

### 第 5 步：自檢清單（品牌層檢查，逐項核）
- [ ] 螢光筆：每頁詞級 ≤3 處、句級 ≤1 組、無第二色
- [ ] 行尾無 1～2 字孤行（瀏覽器實看，不是猜；必須在「有框」等最窄選配組合下驗證，字數預算以有框欄寬為準）
- [ ] 每頁字級 ≤3 個；多行大標用 64px
- [ ] 每頁頁尾在、位置正確
- [ ] 色票外的顏色沒有出現在版式層
- [ ] 插畫佔比合規；圖位都有描述文字
- [ ] 一頁一件事；同版型沒有連續出現（軟規則，違反需有理由）

### 第 6 步：轉可編輯 PPTX 交付（預設產線終點，2026-07-28 定案）
HTML 填完、自檢過後，**直接轉成可編輯 PPTX 交付編輯人員**，不再經 board／PNG：
```
<裝有 playwright + python-pptx + Pillow 的 python3> \
  <技能根目錄>/system/templates/export-editable-pptx.py \
  P1.html P2.html ... [--out 檔名.pptx]
```
不給 `--out` 時，預設輸出到第一個輸入檔同層目錄的「可編輯版.pptx」。
PPTX 產出後，照第 4 步末「狀態文件」規則寫入 `階段: PPT階段`。
產出是真文字框（可雙擊改字）；背景接近純色的頁用原生色塊填充，有真實圖形內容
（如岔路圖線條）的頁才嵌圖；全程記憶體處理背景截圖，不落地中繼檔。

**產線分工與定稿地（2026-07-28 使用者定案）**：
- **定稿地在 PPT**：編輯人員在 Keynote 做最後修整（挪位置、調留白、順稿、改字）。
  改完的 PPT 就是最終版本；HTML 是草稿鷹架，編輯人員的修改**不回寫** HTML，
  HTML 過期即過期。技能端不得再拿 HTML 出圖蓋掉編輯人員的定稿
- **正式 PNG 由編輯人員從 Keynote 批次輸出**（檔案 → 輸出至 → 影像），
  再交出圖人員拿 PNG 去 ChatGPT／NotebookLM 把插圖預留框補上生成圖，補完上傳社群
- **插圖預留框必須帶灰字說明**（例：「插圖：上課東張西望的小孩」）——
  下游出圖人員靠這行字知道在哪裡生什麼圖，不能省

**PPTX 轉檔規格**：
- `.body`（敘事段落）文字框套固定覆寫（2026-07-28 使用者在 Keynote 檢視後定案）：
  **28pt 字級、1.3 倍行距、字距 13%、寬 850px 置中**（左右各留 115px）；
  段落高度與螢光筆位置按新字級字距**重新量測**，非概估。
  其他文字類型（大標、說明字、頁尾、藥丸、引言……）不受影響
- 詞級螢光筆是黃色圓角**色塊（圖形物件）**墊在文字下，不用 pptx 的文字高亮屬性——
  Keynote 不支援該屬性，開啟會直接消失
- 句級螢光筆（`.hl-line`）：黃底圓角文字框，文字直接在框內、上下置中、
  行距 1.3、字距 13%，框尺寸按新字距重新量測置中擺放，上下各加 8pt 內距
- SVG 圖表（循環圖、岔路線…）抽成**透明底 PNG 獨立圖片物件**，不烙進背景，
  交付後可在 Keynote 單獨移動；抽出後背景多能回歸原生純色填充
- 滿版頁規則：非封面、第一個物件貼頁頂邊距（54pt）開始的頁，
  首物件一律下移到 **80pt** 起始
- 插畫位／照片位（`.illust-slot`／`.photo-slot`）：**直角**單一文字框、
  灰虛線框 1.5pt、無陰影、25pt、文字上下置中
- **圓角與單一物件的取捨（2026-07-29 定案）**：Keynote 匯入器遇「圓角＋可見底色
  或框線」必拆成兩層群組——實驗證實連 Keynote 自家輸出的 pptx 重新匯入也一樣，
  無 XML 寫法可繞過。定案：插畫位改直角換單一物件（終會被生成圖蓋掉，視覺損失零）；
  膠囊與句級螢光框保留圓角、接受群組（品牌視覺語彙優先）。直角文字框的底色與
  虛線框都能保留在單一物件上，不受此限制影響
- 字型：本機要裝好 `jf-jinxuan` 各字重 OpenType 家族（ExtraBold／Bold／Medium／
  Book／UltraLight，各自獨立家族名，不是同家族的粗細變化），沒裝會退回系統字

### board.html 試衣間（2026-07-30 改列第 4 步預設產出，此處只留客製用法）
指令與參數見第 1 步。標準流程已在第 4 步自動組一份預設版（gray/無框/無格）；
這裡的手動指令只在使用者要客製標籤、換預設主題、或裁決大改後想單獨重跑一次時才用。

### 選配：HTML 直出 PNG（簡單篇不進 PPT 改稿時才用，2026-07-23 定）
正式 PNG 已改由編輯人員從 Keynote 輸出；只有使用者明說這篇不進 PPT、
要直接從 HTML 出圖時才用：
```
<裝有 playwright 的 python3> <技能根目錄>/system/templates/export.py \
  P1.html P2.html ... [--out 目錄] [--scale 2]
```
（擷取 `.page` 元素，恆為 1080×1350；`--scale 2` 出 2160×2700；
playwright 驅動本機 Chrome（channel=chrome），無需另外下載瀏覽器）
插畫需求若使用者問起，風格描述照 `components.md` 的 C12，產圖交給 image skill 或使用者。

## 禁止事項
- 不准為了找原始檔而全專案搜尋或逐檔翻找；原始檔的有無，以使用者主動提供為準
- 不准跳過工單裁決暫停點直接產出（唯一暫停點；PPTX 交付即完成，不設第二暫停點）
- 不准在編輯人員 PPT 定稿後，再拿 HTML 出圖或出新 PPTX 蓋掉定稿（定稿地在 PPT，單向交棒）
- 不准為了塞內容縮字級、擠留白、加第四個字級
- 不准動 `design-tokens.md` 的色票與字型（要改先徵得使用者同意）
- 不准在版式層使用色票以外的顏色，螢光筆不准出現第二色
- 不准把左欄標記區（數字或圖）拿來放文字

## 失敗處置
- 內容倒不進任何版型：不硬塞。回報使用者，給兩個選項——改寫內容長出形狀，或自組版型
- 字數超預算：改寫句子（優先）；真的改不了，回報並建議拆頁
- 版型庫與規格文件不一致：以 `design-tokens.md`／`components.md` 為準，並回報差異
- 瀏覽器預覽異常（空白快照）：關閉重開 preview，或把目標頁暫換到已授權檔案路徑預覽
- PPTX 進 Keynote 後物件行為存疑（拆群組、樣式跑掉）：用 keynote-inspector 技能
  驅動 Keynote 開檔清點物件數驗證，不靠肉眼猜、不反覆請使用者截圖回報
