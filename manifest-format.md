# 工單格式（每頁一張）

工單是編頁的交付物：使用者裁決用它，填模板照它施工。一組輪播 = 一份工單清單。

## 工單三檔制（2026-07-28 定）

每個任務固定產出三個 .md 檔，建立時機為「編頁完成、進裁決之前」，三檔一次建齊
並存進任務資料夾（存檔位置規則見 SKILL.md 第 4 步：預設 Obsidian Vault 的
`_排版小幫手/<YYYY-MM-DD 專案名>/`，無 OBS 時改存 AI 專案根目錄的 `_排版小幫手/`）：

| 檔名 | 內容 | 給誰看 |
|------|------|--------|
| `工單-結構化.md` | 本檔「欄位」章節的完整欄位格式，內部事實依據 | 填模板時照它施工；使用者主動要才展示 |
| `工單-人讀版.md` | 四行摘要＋各頁工單（人讀版轉譯格式），檔尾依序附「濃縮與刪減申報」「順稿紀錄」「待裁決事項」 | 使用者過目與直接修改的預設版本 |
| `重點-建議-待決.md` | 素材盤點、新形狀提案 | 使用者裁決素材與尺度用 |

裁決有改動時三檔同步更新（含頁碼重編），在原路徑覆寫，不另開新檔名。

## 欄位

| 欄位 | 說明 | 必填 |
|------|------|------|
| 頁碼 | 第幾頁／共幾頁 | ✓ |
| 任務 | 這頁要對讀者做什麼（一句話） | ✓ |
| 唯一主重點 | 只能一個；寫得出兩個就拆頁 | ✓ |
| 版型 | 庫內代號（如 L6），或「自組」＋區塊結構描述 | ✓ |
| slot 文字 | 每個欄位的實際文字，照字數預算斷好句 | ✓ |
| 螢光筆 | 詞級：列出要標的詞（≤3）；句級：標整句（≤1 組） | ✓ |
| 圖位 | 每個圖位一句描述（沒有圖位就寫「無」） | ✓ |
| 銜接 | 上一頁怎麼進來、怎麼丟給下一頁 | 建議 |

## 範例（普世內容示範）

```
【第 2 頁／共 6 頁】
任務：讓讀者發現自己也有這個習慣
唯一主重點：口渴才喝水，其實已經晚了
版型：L6（兩個對象平行比較）
slot 文字：
  頁標：他們都等到「口渴」<br>才想起喝水
  左卡小標：上班族 A
  左卡內文：忙起來就忘，<br>口乾才灌一大杯。
  右卡小標：健身的 B
  右卡內文：只在運動後喝，<br>平時幾乎不碰水。
  底部結論：口渴是身體的<br>最後通牒，不是提醒。
螢光筆：詞級——「口渴」（頁標）、最後通牒（結論）
圖位：左卡上方——盯著電腦的人；右卡上方——擦汗喝水的人
銜接：承接第 1 頁的提問；結論句丟出「為什麼晚了」引導翻頁
```

## 人讀版（預設顯示格式，2026-07-27 定）

上面的結構化工單是內部事實依據：永遠先產生、永遠保留，第 4 步填模板要靠它。
但**不直接拿給使用者看**。使用者看到的預設輸出，是由結構化工單轉譯出的「人讀版」——
用 markdown 語法模擬版面上真正的大小字層次，比純欄位列表更接近實際頁面的樣子。

**只轉譯「slot 文字」「螢光筆」「圖位」三欄**；任務、唯一主重點、版型代號、銜接是
內部產製欄位，人讀版不顯示（使用者主動要求看原始欄位格式時才給結構化工單原文）。

轉譯規則：

| 結構化工單內容 | 人讀版寫法 |
|---|---|
| 主標、頁標、封面雙行標題等 hero／h1 級文字 | `#` 或 `##` 標題（多行標題每行各自開一個 `#`） |
| 內文、卡片內文等一般段落 | 純段落文字 |
| 螢光筆標記的詞或句 | 對應文字改 **粗體**（不用顏色語法，聊天環境不支援 markdown 上色，emoji 色塊也不用——2026-07-27 試過，決定不採用） |
| C8 引言黑線框、C11 CTA 黃條這類「整段框住」的元件 | `>` 引用區塊 |
| 英文小標、頁尾、icon 補充說明、系列標籤、圖位備註等小字類內容 | 用行內代碼包住，如 `` `圖｜精品店櫃檯` `` |
| 圖位描述 | 維持文字備註，不自動轉 emoji（emoji 化屬單頁例外，需使用者當頁指定，不是通則） |

範例——結構化工單：

```
【第 1 頁／共 7 頁】
任務：用標題本身的矛盾（沒說謊＋謊言）勾起好奇
唯一主重點：「沒說謊」也能是一種操縱
版型：L1（標準版）
slot 文字：
  系列標籤：沒說謊的謊言．框架效應篇（待確認）
  主標：沒說謊的<br>謊言
  副標：✦ 框架效應 ✦ 一場合法的操縱
螢光筆：詞級——「謊言」（主標）
圖位：主插畫——精品店櫃檯,店員微笑遞出茶色包,客人若有所思接過
銜接：開篇鉤子；副標「合法的操縱」丟懸念
```

轉譯後的人讀版：

```
`系列標籤｜沒說謊的謊言．框架效應篇（待確認）`

# 沒說謊的**謊言**

### ✦ 框架效應 ✦ 一場合法的操縱

`圖｜精品店櫃檯，店員微笑遞出茶色包，客人若有所思接過`
```

人讀版交付格式：每頁用 `### 第 N 頁` 分隔，一次列完（不要一頁一頁分開問）；
開頭四行摘要（主題、輸出選配、敘事結構、總頁數與版型序列）跟結構化工單相同，照樣附上。
**頁與頁之間插入 `---` 分隔線（2026-07-28 定）**：每個 `### 第 N 頁` 標題前都加一條，
四行摘要與第 1 頁之間、最末頁與檔尾「濃縮與刪減申報」之間也算在內；聊天環境會把
`---` 渲成橫線，讓長串頁面在螢幕上讀起來一頁是一頁，不用靠標題大小分辨頁界。

**素材盤點區塊（2026-07-27 定；2026-07-28 起寫入「重點-建議-待決.md」，不再附在人讀版內）**：每篇必做，格式為按強度排序的
清單，每組寫六項——素材名稱、結構類型、分析標籤（寫機制不寫名詞）、對應元件
（**禁用裸代號**，必須附一行白話描述：長什麼樣、佔多大版面）、尺度（**整頁主角**或
**頁內配件**，全文機制核心的素材預設建議整頁）、建議用頁。庫內沒有合身容器的素材
不准硬塞，改寫「新形狀提案」。判別問句清單見 SKILL.md 第 2 步。沒找到就明寫
「沒找到」。示範（普世例）：

```
① 三選項組｜基本款→進階款→旗艦款
結構：選擇集，銷售話術把三個方案排成引導路徑
標籤：低價入門：先降低門檻／主推款：規格剛好／錨點款：讓中間看起來划算
對應：C9 三欄組（三格並排，上圖下標籤，橫向一排）
尺度：頁內配件（墊在方案介紹頁下緣，約佔 1/4 高）
建議用頁：方案介紹頁
```

**濃縮與刪減申報區塊（2026-07-27 定；2026-07-28 起附在「工單-人讀版.md」檔尾，後接「順稿紀錄」「待裁決事項」兩段）**：所有未逐字保留的原文
一項一行列出（濃縮進哪頁、或未保留），交使用者裁決；完全零刪減就明寫「無刪減」。
使用者裁決刪除的內容也記進申報（標明「使用者裁決刪除」＋日期），讓內容去向可追。
規則出處見 SKILL.md 最高原則「溝通力優先」。

**順稿紀錄區塊（2026-07-28 定，附在申報之後）**：AI 在使用者修改稿上做的微調
（頁碼重編、斷行調整、去除重複字樣、格式修正），一項一行，每項都必須可還原；
沒有微調就明寫「無」。

**待裁決事項區塊（2026-07-28 定，附在人讀版檔案最末）**：所有尚未定案的選項一項一行，
每項必附預設值（使用者不回覆就照預設施工）；全部定案就明寫「無」。

**堆疊序列語言（2026-07-28 物件制）**：結構化工單的「版型」欄可填任務實例代號
（L7），也可直接寫物件堆疊序列，每個物件標明變化與檔位，格式
「物件｜變化｜檔位」（例：膠囊組｜兩行×4顆｜中塊）。裁決指令也用同一套座標：
「降檔」「改田字」「加箭頭」「併頁後循環圖降中塊」。

**【新擬】標示（2026-07-27 定，挪自 V2）**：原文逐字保留是預設；只有頁標、過渡句、
CTA、圖註這類「設計加上去的」文字可以新擬，凡不是原文的字，在工單中一律標【新擬】
（例：分類岔路頁補的對照組職業清單）。與申報區的分工：申報管「刪掉的」，【新擬】管
「加上去的」，兩者合起來才是完整的內容誠信制度。

## 交付格式（結構化工單）

整份工單一次列完（不要一頁一頁分開問），開頭附四行摘要：
主題（螢光筆灰色／螢光暖色／螢光粉色）、輸出選配（線框：有／無；格線：主題預設／全有／全無）、敘事結構、總頁數與版型序列（例：L1b → L2 → L6 → L9 → L10）。
